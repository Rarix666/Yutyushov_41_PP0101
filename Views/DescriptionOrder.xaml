<Window x:Class="AISDisciplineDesc.DescriptionOrder"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        xmlns:local="clr-namespace:AISDisciplineDesc.Converters"
        Title="Просмотр документа" 
        Height="700" 
        Width="900"
        MinHeight="500"
        MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Наименование:" FontSize="20" FontWeight="Bold" Margin="0,0,20,0"/>
            <Label Grid.Column="1" Content="{Binding DocumentName}" FontSize="18" FontWeight="SemiBold"/>
        </Grid>

        <!-- Контент: текст или PDF -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <FlowDocumentScrollViewer 
                Visibility="{Binding IsPdf, Converter={StaticResource InverseBoolToVisibility}}"
                FontSize="16"
                VerticalScrollBarVisibility="Auto"
                BorderThickness="1"
                BorderBrush="#FF707070"
                Background="#FFFEFE">
                <FlowDocument>
                    <Paragraph>
                        <Run Text="{Binding DocumentText}"/>
                    </Paragraph>
                </FlowDocument>
            </FlowDocumentScrollViewer>

            <!-- Контейнер для PDF (WinForms) -->
            <wfi:WindowsFormsHost x:Name="PdfHost" 
                                  Visibility="{Binding IsPdf, Converter={StaticResource BoolToVisibility}}"/>
        </Grid>

        <Button Grid.Row="2" Command="{Binding CloseCommand}" Content="Закрыть" 
                Width="120" Height="40" FontSize="16" HorizontalAlignment="Right" Margin="0,5,0,5"/>
    </Grid>
</Window>
